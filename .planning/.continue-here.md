---
context: default
phase: impeccable-audit-ui-fixes
task: null
total_tasks: 21
status: in_progress
last_updated: 2026-05-29T21:50:17.248Z
---

# Contexte de session — VTC HUB UI Audit & Fixes

Pas de phase GSD active. Session de travail `/impeccable audit` suivi des corrections en cascade.

## État courant

Audit `/impeccable audit` complet sur le codebase VTC HUB. Score initial **8/20 (Poor)**.
Toutes les corrections P0/P1/P2 ont été appliquées. Score estimé post-corrections : **14-15/20 (Good)**.

### PRODUCT.md créé

- **Register** : product
- **Users** : chauffeurs VTC indépendants, France, usage mobile en conditions terrain
- **Brand** : Confiant · Moderne · Fluide
- **Anti-refs** : Uber/Lyft, Notion/Linear, dashboards criards, sites gov FR
- **A11y** : WCAG AA minimum

<current_state>
Corrections audit appliquées et build validé. Dernier fix en date : padding-bottom dashboard
corrigé pour que le contenu scrollable ne passe pas sous le dock "Prochaines Missions" fixe.

Le dock est `fixed bottom-16 md:bottom-0` avec hauteur ~130px.
Mobile pb = `calc(13rem + env(safe-area-inset-bottom, 0px))` ≈ 208px.
Desktop pb = `pb-36` = 144px.

User a ensuite demandé de vérifier l'affichage de la prochaine mission dans le dock.
La carte booking dans le dock (ligne ~340 de dashboard.astro) utilise `upcomingBookings.slice(0,1)`.
Un `text-[6px]` sur le mois a été corrigé en `text-[9px]`.
</current_state>

<completed_work>

### Corrections audit appliquées (25 fichiers modifiés)

**P0 — Accessibilité critique**
- [x] Gradient text supprimé sur TOUS les headings (index, login, signup ×3) → `text-white`
- [x] `animate-ping` → `motion-safe:animate-ping` (index.astro)
- [x] `Button.astro` : `text-[8px]` → `text-xs`, `tracking-widest` → `tracking-wider`, `min-h-[44px]`
- [x] Bottom nav labels : `text-[8px]` → `text-[10px]` + `font-semibold` (AppLayout.astro)

**P1 — Majeur**
- [x] Login : submit button aligné avec Button.astro, labels `slate-500→slate-400`, placeholders `slate-600→slate-500`, `role="alert"` sur `#message`
- [x] Signup : boutons `text-[11px]→text-xs min-h-[44px]`, labels `slate-400`, `role="alert"`, headings sans gradient
- [x] AppLayout : header mobile + sidebar desktop = fond solide (plus de backdrop-blur) ; modals gardent le blur
- [x] Layout.astro : supprimé les `--accent` Astro-default conflictuels (`#883AEA` vs `#6366f1`), fond `#13151a→#050505`, `class="dark"` ajouté

**P2 — Mineur**
- [x] AppLayout labels sidebar/header : `text-[10px] tracking-widest` → `text-xs tracking-wider`
- [x] Dashboard : badge `text-[7px]→text-[10px]`, labels `text-[8px]→text-[10px]`
- [x] Bulk sed : `text-[7/8/9px]→text-[10px]` sur 15 fichiers (pages app + composants)
- [x] Dashboard `pb-44` → `pb-[calc(13rem+safe-area)] md:pb-36` (couvre dock + nav)
- [x] Carte booking dock : `text-[6px]→text-[9px]`
</completed_work>

<remaining_work>

### Reste à faire (P2-P3 + refontes demandées)

**P2 restants**
- [ ] Form labels `text-slate-500` dans `setup.astro` + `pricing.astro` → `text-slate-400` (contraste borderline 4.4:1)
- [ ] `tracking-widest` sur labels désormais 10px dans setup/pricing/bookings → `tracking-wider`

**P3**
- [ ] `autocomplete="email"` et `autocomplete="current-password"` sur inputs login/signup
- [ ] Vérifier `AdminLayout.astro` (si il existe) : `class="dark"` manquant ?

**Refontes demandées par l'user (session en cours)**
- [ ] Refonte page login — `/impeccable craft login` (Confiant·Moderne·Fluide, mobile-first)
- [ ] Refonte tunnel signup/onboarding — `/impeccable craft signup`
- [ ] Vérifier en conditions réelles que la carte "Prochaine Mission" s'affiche dans le dock quand une réservation existe (test manuel avec données réelles)

**Ajustement possible**
- [ ] Recheck padding-bottom dashboard sur vrai appareil iOS (safe-area variable selon modèle)
</remaining_work>

<decisions_made>

- Brand Confiant·Moderne·Fluide (option 3 au choix utilisateur)
- Register product (app B2B, design SERVES the product)
- Suppression backdrop-blur header/sidebar : perf GPU mobile, garder uniquement modals
- Fond aligné `#050505` sur Layout.astro (pages publiques) pour cohérence avec AppLayout
- `text-slate-400` comme minimum pour labels de formulaire (contraste ~6.5:1 vs slate-500 ~4.4:1)
- `tracking-wider` remplace `tracking-widest` sur les éléments UI de taille ≤12px
</decisions_made>

<blockers>
Aucun bloquant technique. Un point à vérifier manuellement : hauteur exacte du dock "Prochaines Missions" sur vrai appareil pour valider le padding-bottom.
</blockers>

## Required Reading (in order)
1. `PRODUCT.md` — contexte brand et register créé cette session
2. `.planning/HANDOFF.json` — état complet machine-readable

## Infrastructure State
- Dev server : était sur port 4322 (vite cache purgé pendant la session, à relancer avec `npm run dev`)
- Build : validé `npm run build` → succès, 0 erreur Astro
- TS errors : pré-existantes dans supabase/functions (Deno) et un null-check dans terrain-transition.ts — NON causées par cette session

## Files Modified (non commités)
25 fichiers — voir `git status` ou `HANDOFF.json.uncommitted_files`

<context>
Session /impeccable audit → corrections en cascade. Le codebase avait 3 violations de banissement absolu
actives (gradient text partout, glassmorphism par défaut, tiny uppercase endemic). Toutes corrigées.
L'user a demandé en fin de session une refonte de login + signup + "destock global" avec le brand
Confiant·Moderne·Fluide. Ces refontes n'ont pas encore commencé.
</context>

<next_action>
1. `npm run dev` pour relancer le serveur
2. Vérifier visuellement le dashboard : dock visible + contenu ne passe pas dessous
3. Tester la carte "Prochaine Mission" avec une vraie réservation en DB
4. Puis attaquer : `/impeccable craft login` pour la refonte login
</next_action>
