---
// src/pages/app/dashboard.astro
import AppLayout from "../../layouts/AppLayout.astro";
import { supabase } from "../../lib/supabase";

const { profile } = Astro.locals;

if (!profile) return Astro.redirect("/login");

// 📅 Calcul des dates pour les KPIs
const now = new Date();
const startOfToday = new Date(
    now.getFullYear(),
    now.getMonth(),
    now.getDate(),
).toISOString();
const startOfMonth = new Date(
    now.getFullYear(),
    now.getMonth(),
    1,
).toISOString();

// 1️⃣ Courses du jour
const { count: dailyCount } = await supabase
    .from("bookings")
    .select("*", { count: "exact", head: true })
    .eq("current_tenant_id", profile.tenant_id)
    .gte("pickup_time", startOfToday);

// 2️⃣ Chiffre d'affaires du mois
const { data: monthBookings } = await supabase
    .from("bookings")
    .select("total_amount")
    .eq("current_tenant_id", profile.tenant_id)
    .gte("pickup_time", startOfMonth)
    .neq("status", "cancelled");

const monthlyRevenue =
    monthBookings?.reduce((acc, curr) => acc + Number(curr.total_amount), 0) ||
    0;

// 3️⃣ Dernières courses pour l'affichage rapide (V1)
const { data: recentBookings } = await supabase
    .from("bookings")
    .select("*")
    .eq("current_tenant_id", profile.tenant_id)
    .order("created_at", { ascending: false })
    .limit(5);
---

<AppLayout title="Dashboard">
    <div class="flex-1 flex flex-col p-12 overflow-hidden h-full">
        <!-- FIXED TOP SECTION -->
        <div class="flex-shrink-0 border-b border-white/5 pb-10 mb-10">
            <div class="mb-10">
                <span
                    class="px-3 py-1 bg-indigo-500/10 border border-indigo-500/20 rounded-full text-[10px] font-black tracking-widest text-indigo-400 uppercase"
                >
                    {profile?.tenant_role || "Chauffeur"}
                </span>
                <h1
                    class="text-4xl font-black mt-2 tracking-tighter italic uppercase text-white"
                >
                    Tableau de bord
                </h1>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div
                    class="glass p-8 rounded-3xl border border-white/5 transition-all hover:border-indigo-500/30 group bg-white/[0.02]"
                >
                    <p
                        class="text-slate-500 text-[10px] font-black uppercase tracking-[0.2em] mb-3 group-hover:text-indigo-400 transition-colors"
                    >
                        CA du Mois
                    </p>
                    <p class="text-4xl font-black tabular-nums text-white">
                        {
                            monthlyRevenue.toLocaleString("fr-FR", {
                                minimumFractionDigits: 2,
                            })
                        } €
                    </p>
                </div>

                <div
                    class="glass p-8 rounded-3xl border border-white/5 bg-white/[0.02]"
                >
                    <p
                        class="text-slate-500 text-[10px] font-black uppercase tracking-[0.2em] mb-3"
                    >
                        Courses du Jour
                    </p>
                    <p class="text-4xl font-black tabular-nums text-white">
                        {dailyCount || 0}
                    </p>
                </div>

                <div
                    class="glass p-8 rounded-3xl border border-white/5 bg-white/[0.02]"
                >
                    <p
                        class="text-slate-500 text-[10px] font-black uppercase tracking-[0.2em] mb-3"
                    >
                        Note Moyenne
                    </p>
                    <p class="text-4xl font-black tabular-nums text-white">
                        5.0 <span class="text-indigo-500 text-xl">★</span>
                    </p>
                </div>
            </div>
        </div>

        <!-- DYNAMIC CONTENT SECTION -->
        <div class="flex-1 flex flex-col min-h-0 overflow-hidden">
            {
                recentBookings && recentBookings.length > 0 ? (
                    <div class="flex-1 flex flex-col min-h-0">
                        <h3 class="text-lg font-black uppercase italic tracking-tighter flex items-center gap-3 text-white mb-6 flex-shrink-0">
                            <span class="w-2 h-2 rounded-full bg-indigo-500 animate-pulse" />
                            Dernières Activités
                        </h3>
                        <div class="flex-1 overflow-y-auto pr-2 custom-scrollbar">
                            <div class="glass rounded-3xl overflow-hidden border border-white/5 bg-white/[0.01]">
                                <table class="w-full text-left border-collapse">
                                    <thead class="bg-white/5 sticky top-0 z-10">
                                        <tr class="border-b border-white/5">
                                            <th class="px-8 py-4 text-[10px] font-black uppercase tracking-widest text-slate-500">
                                                Détails
                                            </th>
                                            <th class="px-8 py-4 text-right text-[10px] font-black uppercase tracking-widest text-slate-500">
                                                Statut / Prix
                                            </th>
                                        </tr>
                                    </thead>
                                    <tbody class="divide-y divide-white/5">
                                        {recentBookings.map((booking) => (
                                            <tr class="hover:bg-white/5 transition-colors group">
                                                <td class="px-8 py-8">
                                                    <div class="font-bold text-white uppercase italic tracking-tighter text-lg">
                                                        {booking.client_name}
                                                    </div>
                                                    <div class="text-[10px] text-slate-500 font-bold uppercase mt-2 group-hover:text-slate-300 transition-colors">
                                                        {booking.pickup_address}
                                                    </div>
                                                </td>
                                                <td class="px-8 py-8 text-right underline-offset-4">
                                                    <span
                                                        class={`px-3 py-1.5 rounded-lg text-[10px] font-black uppercase tracking-widest ${
                                                            booking.status ===
                                                            "completed"
                                                                ? "bg-emerald-500/10 text-emerald-500 border border-emerald-500/20"
                                                                : booking.status ===
                                                                    "cancelled"
                                                                  ? "bg-red-500/10 text-red-500 border border-red-500/20"
                                                                  : "bg-indigo-500/10 text-indigo-400 border border-indigo-500/20"
                                                        }`}
                                                    >
                                                        {booking.status}
                                                    </span>
                                                    <div class="mt-3 font-black text-2xl text-white tabular-nums">
                                                        {Number(
                                                            booking.total_amount,
                                                        ).toFixed(2)}{" "}
                                                        €
                                                    </div>
                                                </td>
                                            </tr>
                                        ))}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                ) : (
                    <div class="flex-1 flex flex-col items-center justify-center text-center pb-12">
                        <div class="w-24 h-24 bg-white/5 rounded-[2.5rem] flex items-center justify-center mb-8 border border-white/5 shadow-2xl">
                            <svg
                                class="w-12 h-12 text-slate-500"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="1.2"
                                    d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                                />
                            </svg>
                        </div>
                        <h3 class="text-2xl font-black mb-4 uppercase italic tracking-wider text-slate-100">
                            Aucune course pour le moment
                        </h3>
                        <p class="text-slate-500 max-w-sm font-medium leading-relaxed">
                            Votre compte est actif. Vos futures courses
                            s'afficheront ici en temps réel dès leur
                            enregistrement.
                        </p>
                    </div>
                )
            }
        </div>
    </div>
</AppLayout>
